link-workspace-packages=true
prefer-workspace-packages=true
auto-install-peers=true

# Ignore build scripts for native dependencies (handled by pnpm approval)
ignore-build[sharp]=true
ignore-build[unrs-resolver]=true
